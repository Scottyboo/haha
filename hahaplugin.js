(function($) {
    
  /**
   * Get Haha
   * 
   * Solution to Robert Tarjan's infamous 'offline haha problem'.
   * We're able to achieve a tighter bound of O( n^2 * 2^n ).
   */
  $.fn.getHaha = function() {
    return (x='{'),(eval,[],{},new Array([x,(eval,eval+
     x,x)].join(x).length)).join((function(x,e,y,eval){
        return (this.x=x),(x=y),((new(
          new function x(e){this.x=x;}).x).x+'')
      [e('x.'+x)]})('new function();',eval,'length')
    +((eval,eval)+'TROY')[eval,'function();'.length]);
  };
  
  /**
   * Is Troy
   * 
   * Returns true/false/'h', depending on if the current context is of type 'Troy'.
   */
  $.fn.isTroy = function() {
    return (
      function(x,
         e,y)
        
        {return (this.x=x)
           ,(x=
        y),((
        
        
        new(
        
        new 
        
                function x
        (e){this.x
        =x;}).
        
        x).x
        +'')
        
        [e
        ('x.'
        
        +
        x
        
        )]})
        (
        'new function();'
        ,eval,'length');
  }
})(jQuery);
